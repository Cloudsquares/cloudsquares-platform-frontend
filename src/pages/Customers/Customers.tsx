import { CustomersModule } from "../../modules/CustomersModule";

export const Customers = () => {
  return (
    <section data-testid="pageCustomers">
      <CustomersModule />
    </section>
  );
};
