import { Button } from "@/shared/components/ui";

export const PropertiesFilter = () => {
  return (
    <div>
      <Button type="button" size="md">
        Фильтры
      </Button>
    </div>
  );
};
